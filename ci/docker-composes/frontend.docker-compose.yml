version: "3.4"

services:
  frontend:
    build:
      context: /../../
      dockerfile: /ci/dockerfiles/Frontend.Dockerfile
    environment:
      NODE_ENV : development
      REACT_APP_API_URL: 'http://webapi:8000'
    ports:
      - "3000:80"
    volumes:
      - .:/app