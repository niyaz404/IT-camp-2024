/**
 * Округляет координату элемента
 * @param coordinate координата
 * @returns округленная координата
 */
export const formatCoordinate = (coordinate: number) => {
  return Math.ceil(coordinate);
};
